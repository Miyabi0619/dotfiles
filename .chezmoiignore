# Source-only files
README.md
.github/
Makefile
bootstrap/
nix/
packages/
.Brewfile
.Brewfile_macos
.chezmoiscripts/

# High-churn application settings. Use app sync or adopt explicitly later.
.claude/
.config/gh/
.config/starship.toml
Library/

# macOS専用ファイル - Linux/Windowsでは無視
{{ if ne .chezmoi.os "darwin" }}
{{ end }}

# Linux専用ファイル - macOS/Windowsでは無視
{{ if ne .chezmoi.os "linux" }}
.bash_profile
.bashrc
.profile
{{ end }}

# Windows専用ファイル - macOS/Linuxでは無視
{{ if ne .chezmoi.os "windows" }}
# 将来Windows専用ファイルを追加する場合はここに
{{ end }}

# 共通で無視するファイル
